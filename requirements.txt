# Core engine and API
pydantic>=2.8
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27

# Document ingestion (Stage A)
PyMuPDF>=1.24
paddleocr>=3.7
paddlepaddle>=3.0
reportlab>=4.2

# External evidence (Stage B)
scrapling>=0.4

# Optional cloud backends. The product runs without them on a local store;
# install these only when Firebase or Cloudinary are actually configured.
# firebase-admin>=6.5
# cloudinary>=1.40

# Original Streamlit console (superseded by the React frontend, kept working)
streamlit>=1.38

# Tests
pytest>=8.3
