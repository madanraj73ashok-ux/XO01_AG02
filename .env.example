# Copy to .env and fill in. .env is git-ignored — never commit real keys.
# The engine runs fully offline without these; the LLM layer is optional.

LLM_PROVIDER=gemini
LLM_API_KEY=
LLM_MODEL=gemini-2.0-flash
